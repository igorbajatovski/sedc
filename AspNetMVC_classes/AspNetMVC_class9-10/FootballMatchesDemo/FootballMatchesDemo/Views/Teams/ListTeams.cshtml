@model IEnumerable<Team>
<h2>Teams</h2>

<a asp-controller="Teams" asp-action="AddTeam" class="btn btn-default">Create Team</a>

<br />

@{ 
    string noTeams = "No teams available";
    if (Model.Count() > 0)
    {
        foreach (var team in Model)
        {
            @team.ID;
            @team.Name;
        }
    }
    else
    {
        @noTeams;
    }
}